# Test Configuration
# Disable Rate Limiting for tests
app.ratelimit.enabled=false
cors.allowed-origins=*

# External API Configurations (placeholders for tests)
gemini.api.key=test-key
gemini.api.url=https://test.api.com
alphavantage.api.key=test-key
alphavantage.api.url=https://test.api.com
dropbox.api.key=test-key
google.drive.client-id=test-id
google.drive.client-secret=test-secret
google.drive.redirect-uri=http://localhost:8080/test

# JWT Configuration
jwt.secret=test-secret-key-for-testing-purposes-only-needs-to-be-long-enough
jwt.expiration=3600000
jwt.refresh-expiration=604800000

# Use H2 in-memory database for tests
